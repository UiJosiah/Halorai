# Plugin sample assets

Bundled with the Flask backend for production (Render `rootDir: backend`).

Used by:
- `GET /api/ai/flyer/json`
- `GET /api/ai/flyer/layers.zip`

Files:
- `background.png` — demo background
- `logo.png` — demo logo
- `minister.png` — demo minister portrait (used twice in the 2-minister sample)
